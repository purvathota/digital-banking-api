# Docker/Railway profile overrides
spring:
  datasource:
    url: jdbc:postgresql://${PGHOST}:${PGPORT:5432}/${PGDATABASE}
    username: ${PGUSER}
    password: ${PGPASSWORD}
  data:
    redis:
      url: ${REDIS_URL}
  cache:
    type: redis

server:
  port: ${PORT:8080}
  